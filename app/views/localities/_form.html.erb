<%= form_with(model: locality, local: true) do |form| %>
  <% if locality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(locality.errors.count, "error") %> prohibited this locality from being saved:</h2>

      <ul>
      <% locality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :locality_tipo %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.text_field :precio, id: :locality_precio %>
  </div>

  <div class="field">
    <%= form.label :fecha_limite %>
    <%= form.date_select :fecha_limite, id: :locality_fecha_limite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
