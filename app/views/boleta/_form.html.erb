<%= form_with(model: boletum, local: true) do |form| %>
  <% if boletum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boletum.errors.count, "error") %> prohibited this boletum from being saved:</h2>

      <ul>
      <% boletum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :boletum_nombre %>
  </div>

  <div class="field">
    <%= form.label :ciudad %>
    <%= form.text_field :ciudad, id: :boletum_ciudad %>
  </div>

  <div class="field">
    <%= form.label :pais %>
    <%= form.text_field :pais, id: :boletum_pais %>
  </div>

  <div class="field">
    <%= form.label :fecha_inicio %>
    <%= form.date_select :fecha_inicio, id: :boletum_fecha_inicio %>
  </div>

  <div class="field">
    <%= form.label :fecha_evento %>
    <%= form.date_select :fecha_evento, id: :boletum_fecha_evento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
